<?xml version="1.0" encoding="utf-8"?>
<!-- v192: зона разработки. Темнее основного фона и с сухой рамкой -
     чтобы с первого взгляда было видно: это приборы, не интерфейс. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="#060608" />
    <stroke android:width="1dp" android:color="@color/axis_dim" />
    <corners android:radius="6dp" />
</shape>
